**Basic "Dot" Commands**: These are special instructions for the SQLite shell:

- `.tables` – List all tables in your database.
    
- `.schema table_name` – See the structure of a specific table.
    
- `.header on` / `.mode column` – Makes the output easier to read.
    
- `.quit` – Exit the shell.